#include <iostream>
#include <cmath>
#include "Eigen/Dense"

using namespace std;
using Eigen::MatrixXd;
using Eigen::VectorXd;

int plus_one(int x);

// int main() {    
    // VectorXd a = VectorXd::Zero(3);
    // a(0) = 1;
    // a(1) = 3;
    // // cout << a << "\n";
    
    // cout << a.unaryExpr(&plus_one);
// }

int divint(int, int);  

int main() 
{ 
   int x = 5, y = 2; 
   cout << divint(x, y); 
   
   x =3; y = 0; 
   cout << divint(x, y); 
   
   return 0; 
}  

int divint(int a, int b) 
{ 
   return a / b; 
}   


int plus_one(int x) {
    return x + 1;
}